import{signIn}from"@/auth";export const metadata={title:"ورود مدیریت"};export default function Login(){return <main className="grid min-h-screen place-items-center bg-paper-50"><form action={async(fd)=>{"use server";await signIn("credentials",{email:fd.get("email"),password:fd.get("password"),redirectTo:"/admin/dashboard"})}} className="w-full max-w-md rounded-card bg-white p-8 shadow-xl"><h1 className="text-2xl font-bold">ورود به مدیریت</h1><label className="mt-6 block">ایمیل<input name="email" type="email" required className="mt-2 w-full rounded-button border p-3"/></label><label className="mt-4 block">رمز عبور<input name="password" type="password" required className="mt-2 w-full rounded-button border p-3"/></label><button className="mt-6 w-full rounded-button bg-navy-900 p-3 text-white">ورود</button></form></main>}