import{db}from"@/lib/db";import Link from"next/link";export const dynamic="force-dynamic";export default async function I({searchParams}:{searchParams:{status?:any}}){const xs=await db.inquiry.findMany({where:searchParams.status?{status:searchParams.status}:{},orderBy:{createdAt:"desc"}});return <><h1 className="text-3xl font-bold">درخواست‌ها</h1><div className="my-5 flex gap-3"><Link href="?">همه</Link><Link href="?status=NEW">جدید</Link><Link href="?status=CONTACTED">تماس گرفته</Link><Link href="?status=CLOSED">بسته</Link></div>{xs.map(x=><Link key={x.id} href={`/admin/inquiries/${x.id}`} className="mb-3 block rounded-card bg-white p-5 shadow"><b>{x.name}</b> — {x.phone}<span className="float-left">{x.status}</span></Link>)}</>}