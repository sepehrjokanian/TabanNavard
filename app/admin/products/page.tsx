import{db}from"@/lib/db";import{toggleProduct,saveProduct}from"./actions";export const dynamic="force-dynamic";export default async function P({searchParams}:{searchParams:{q?:string}}){const items=await db.product.findMany({where:searchParams.q?{title:{contains:searchParams.q,mode:"insensitive"}}:{},orderBy:{createdAt:"desc"}});return <><h1 className="text-3xl font-bold">مدیریت محصولات</h1><form className="mt-5"><input name="q" placeholder="جستجو" className="rounded-button border p-3"/></form><details className="my-6 rounded-card bg-white p-5"><summary className="font-bold">افزودن محصول</summary><form action={saveProduct} className="mt-5 grid gap-3 md:grid-cols-2">{['title','slug','subtype','price','shortDesc','longDesc'].map(x=><input key={x} name={x} required={!['subtype','price'].includes(x)} placeholder={x} className="rounded-button border p-3"/>)}<select name="category" className="border p-3"><option value="ELEVATOR">آسانسور</option><option value="SPARE_PART">قطعه</option></select><textarea name="specs" defaultValue='{"ظرفیت":"۶۳۰ کیلوگرم"}' className="border p-3"/><label><input type="checkbox" name="isFeatured"/> ویژه</label><button className="bg-blue-600 p-3 text-white">ذخیره</button></form></details><div className="overflow-x-auto"><table className="w-full bg-white"><tbody>{items.map(x=><tr key={x.id} className="border-b"><td className="p-4">{x.title}</td><td>{x.category}</td><td>{x.isActive?"فعال":"غیرفعال"}</td><td><form action={toggleProduct}><input type="hidden" name="id" value={x.id}/><button className="text-blue-600">تغییر وضعیت</button></form></td></tr>)}</tbody></table></div></>}