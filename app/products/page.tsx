import{db}from"@/lib/db";import{Nav,Footer,Section}from"@/components/ui";import{ProductCard}from"@/components/product-card";export const revalidate=600;export const metadata={title:"محصولات"};export default async function Products({searchParams}:{searchParams:{category?:string;min?:string;max?:string;q?:string}}){const s=searchParams;const where:any={isActive:true};if(s.category)where.category=s.category;if(s.min||s.max)where.price={gte:s.min?Number(s.min):undefined,lte:s.max?Number(s.max):undefined};if(s.q)where.OR=[{title:{contains:s.q,mode:"insensitive"}},{subtype:{contains:s.q,mode:"insensitive"}}];const items=await db.product.findMany({where,orderBy:{createdAt:"desc"}});return <><Nav/><Section><h1 className="text-4xl font-extrabold">محصولات و قطعات</h1><form className="my-8 grid gap-3 rounded-card bg-white p-5 md:grid-cols-4"><input name="q" placeholder="جستجو" className="rounded-button border p-3"/><select name="category" className="rounded-button border p-3"><option value="">همه دسته‌ها</option><option value="ELEVATOR">آسانسور</option><option value="SPARE_PART">قطعات</option></select><input name="min" type="number" placeholder="حداقل قیمت" className="rounded-button border p-3"/><button className="rounded-button bg-blue-600 text-white">اعمال فیلتر</button></form><div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{items.map(p=><ProductCard key={p.id} p={p}/>)}</div>{!items.length&&<p>محصولی مطابق فیلترها یافت نشد.</p>}</Section><Footer/></>}