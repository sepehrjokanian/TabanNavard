import{notFound}from"next/navigation";import{db}from"@/lib/db";import{Nav,Footer,Section,Placeholder,Card}from"@/components/ui";import{ContactForm}from"@/components/contact-form";import{money}from"@/lib/utils";export const revalidate=600;export async function generateMetadata({params}:{params:{slug:string}}){const p=await db.product.findUnique({where:{slug:params.slug}});return{title:p?.title||"محصول"}}export default async function Detail({params}:{params:{slug:string}}){const p=await db.product.findUnique({where:{slug:params.slug},include:{images:true}});if(!p||!p.isActive)notFound();const specs=p.specs as Record<string,string>;return <><Nav/><Section><div className="grid gap-10 md:grid-cols-2"><Placeholder label={`گالری ${p.title}`}/><div><h1 className="text-4xl font-extrabold">{p.title}</h1><p className="mt-5 leading-8">{p.longDesc}</p><b className="mt-6 block text-xl text-blue-600">{money(p.price)}</b></div></div><div className="mt-16 grid gap-10 md:grid-cols-2"><Card><h2 className="text-2xl font-bold">مشخصات فنی</h2><dl className="mt-4">{Object.entries(specs).map(([k,v])=><div className="flex justify-between border-b py-3" key={k}><dt>{k}</dt><dd>{v}</dd></div>)}</dl></Card><Card><h2 className="mb-5 text-2xl font-bold">درخواست پیش‌فاکتور</h2><ContactForm productId={p.id} source="PRODUCT"/></Card></div></Section><Footer/></>}