import {z}from"zod";export const inquirySchema=z.object({name:z.string().min(2,"نام را وارد کنید"),phone:z.string().regex(/^[۰-۹0-9+\-\s]{7,20}$/,"شماره تماس معتبر نیست"),email:z.string().email("ایمیل معتبر نیست").optional().or(z.literal("")),message:z.string().min(10,"پیام باید حداقل ۱۰ حرف باشد"),productId:z.string().optional(),source:z.enum(["PRODUCT","CONTACT_FORM"]),website:z.string().max(0).optional()});