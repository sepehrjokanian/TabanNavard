import NextAuth from"next-auth";import Credentials from"next-auth/providers/credentials";import bcrypt from"bcryptjs";import{z}from"zod";import{db}from"@/lib/db";export const{handlers,auth,signIn,signOut}=NextAuth({session:{strategy:"jwt"},pages:{signIn:"/admin/login"},providers:[Credentials({credentials:{email:{},password:{}},authorize:async(c)=>{const x=z.object({email:z.string().email(),password:z.string().min(8)}).safeParse(c);if(!x.success)return null;const u=await db.adminUser.findUnique({where:{email:x.data.email}});if(!u||!await bcrypt.compare(x.data.password,u.passwordHash))return null;return{id:u.id,email:u.email,name:u.name}}})]});